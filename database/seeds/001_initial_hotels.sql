-- Production seed intentionally creates no hotels.
-- Create hotels and hotel admins from the Super Admin console after bootstrapping
-- the first super admin user.
SELECT 'No demo hotel data inserted. Platform is ready for real onboarding.' AS message;
